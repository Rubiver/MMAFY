# 유저 테스트 실행 방법

프로젝트 루트에서 아래 명령을 각각 별도 터미널에 실행한다.

## 1. 게임 서버 시작

```bash
cd /home/ssafy/work/codex_project/codes
npm run dev:server
```

서버가 정상 시작되면 `게임 서버가 2567 포트에서 실행 중입니다.`가 표시된다.

## 2. 웹 클라이언트 시작

```bash
cd /home/ssafy/work/codex_project/codes
npm run dev
```

브라우저에서 아래 주소를 연다.

```text
http://localhost:5173/
```

여러 명을 시험할 때는 같은 주소를 서로 다른 브라우저 또는 시크릿 창에서 열고, 하나가 방을 만든 뒤 표시된 방 코드로 나머지가 입장한다.

## 테스트 중 확인할 항목

- 개발 서버에서는 이동 속도가 운영 값의 세 배다.
- 처치 후 시체가 남고, 가까운 생존자가 좌클릭으로 신고할 수 있다.
- 마피아는 가까운 시민을 [F] 또는 좌클릭으로 처치할 수 있다.
- 사망자는 하단의 관전 대상 버튼으로 살아 있는 참가자의 시점으로 전환할 수 있다.
- 시민은 보급 상자 또는 납품대 가까이에서 [E]를 눌러 물품을 들고 전달한다. 물품을 든 모습은 다른 참가자 화면에 상자로 표시된다.
- 마피아는 숲·산업 지대 양쪽 환풍구 가까이에서 [E]를 눌러 반대편으로 이동할 수 있다.
- 바리케이드 설치 뒤 참가자가 충돌 범위 안에 끼거나 이동 불가 상태가 되지 않는지 확인한다.

## 종료

서버와 클라이언트를 실행한 각 터미널에서 `Ctrl+C`를 눌러 종료한다.
