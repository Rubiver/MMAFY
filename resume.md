# 작업 재개 메모

## 현재 상태

- 페이즈 1부터 페이즈 5까지의 기본 멀티플레이 흐름을 구현했다. 최대 25명 방, 방 코드 생성·입장, 준비·시작, 무작위 별명, 방장 퇴장·방 삭제, 서버 권한형 실시간 동기화를 제공한다.
- 게임 공간은 가로 180미터·세로 140미터다. 서쪽 숲과 동쪽 산업 지대 사이 강은 두 교량으로만 건널 수 있으며, 나무·건물·강·경계·셔터 충돌은 서버와 클라이언트가 공용 모델로 함께 검증한다.
- 참가자는 걷기·달리기·카메라 회전·사다리·환풍구·장치 상호작용을 할 수 있다. Caps 지도와 Escape 뒤에는 캔버스 포커스와 포인터 잠금을 복구한다.
- 마피아는 서버가 검증한 거리·역할·생존 상태·재사용 대기시간 안에서 시민을 처치한다. 여러 대상은 [Q]로 고르고 [F] 또는 좌클릭으로 요청할 수 있다. 사망자는 시체로 남고 관전 상태가 되며, 가까운 생존자 누구나 금색 강조 시체를 좌클릭해 신고할 수 있다.
- 긴급 회의는 남쪽 중앙 교량의 종을 조준해 [E]로 연다. 신고 또는 종 사용 뒤 90초 동안 생존자만 채팅·투표할 수 있으며, 사망자는 목록에서 붉게 표시한다. 생존자 과반의 건너뛰기는 즉시 결과로 넘어가며, 최다 득표자는 처형한다. 회의 전 위치를 보관해 결과 뒤 다시 플레이 공간으로 복귀한다.
- 건너뛰기 결과 뒤 검은 화면으로 멈추던 오류를 수정했다. `Meeting`의 Hook 호출 순서를 고정하고 회의 중에도 3D 캔버스를 유지한다. Playwright로 `PLAYING` 복귀, 캔버스 1개 유지, 결과 오버레이 제거를 확인했다.
- 시민 공통 임무로 회로 연결 퍼즐을 구현했다. `호박 → 청록 → 보라` 순서 성공은 서버가 검증하며, 참가자별 최초 성공 때 25%가 올라가고 100%면 시민이 승리한다.
- 시설은 발전기 정전·복구, 보안 셔터, CCTV 관제실, 통신 장애·복구, 마피아 환풍구를 구현했다. 마피아는 정전·통신 장애·셔터 잠금으로 고립을 만들 수 있고, 시민은 역할·거리·상태 검증을 거쳐 복구·해제할 수 있다.
- 승리 조건은 마피아 전원 추방 또는 공통 임무 완료 시 시민 승리, 살아 있는 마피아 수가 살아 있는 시민 수 이상일 때 마피아 승리다.
- 최신 검증: 클라이언트 8개, 서버 31개로 총 39개 자동 테스트와 생산 빌드가 통과했다. 회의 복귀 화면은 375px·768px·1280px에서 `docs/screenshots/meeting-skip-recovery-*.png`으로 저장했다.

## 다음 구현 우선순위

1. `READY`: 경보·바리케이드. 시민이 제한 횟수로 동선을 보호하고, 마피아가 우회하거나 해제할 수 있도록 서버 권한으로 구현한다.
2. 물품 운송 임무, 보안 카드 인증, 두 명 이상 협동 임무를 추가한다.
3. 긴급 임무를 추가한다. 제한 시간 내 복구 실패는 마피아 승리, 성공은 시설 정상화로 서버가 확정한다.
4. 실내 구역을 중앙홀·발전실·통신실·의무실·연구실·창고·정비실·격납고로 확장하고, 위험 구역과 환풍구 연결 그래프를 추가한다.
5. 회의에 신고 위치·시간·투표 집계 정보를 보강하고, 관전자 대상 전환과 게임 결과·다음 판 대기실 흐름을 추가한다.
6. PRD의 인원별 마피아 추천표를 정확히 적용하고, 시작·역할·결과 연출을 서버 상태 기계로 정리한다.
7. 25명 통합 부하와 주요 화면의 375px·768px·1280px 검증을 다시 수행한다.

## 작업 규칙과 주의점

- 작업 전 `docs/RULES.md`와 관련 단계·백로그 문서를 읽고 `git status`와 Playwright MCP 연결을 확인한다.
- 결과에 영향을 주는 이동, 상호작용, 임무, 처치, 투표, 승패는 반드시 서버가 검증한다.
- 웹 화면을 바꾸면 Playwright MCP로 375px·768px·1280px 전체 화면을 검증하고 `docs/screenshots/`에 저장한다. 검증 뒤 탭과 MCP 전용 Chrome만 종료한다.
- 구현을 완료하면 `docs/current-phase-log.md`와 이 문서를 갱신하고, 관련 파일만 한국어 커밋으로 남긴다.
- 현재 작업 트리에는 이전 작업의 미커밋 변경이 있다: `AGENTS.md`, `codes/client/src/App.tsx`, `codes/client/src/game/interactions.ts`, `codes/client/src/game/interactions.test.ts`, `codes/client/src/game/localMovement.ts`, `codes/client/src/ui/Hud.tsx`, 보안 셔터 스크린샷과 `.playwright-mcp/`. 다음 작업에서 이 변경을 되돌리거나 무관한 커밋에 포함하지 않는다.
