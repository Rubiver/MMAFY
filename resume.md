# 작업 재개 메모

## 현재 상태

- 페이즈 1부터 페이즈 5까지 완료했다.
- 플레이 공간은 가로 36미터·세로 28미터로 확장됐고, 게임 시작 때 서버가 25개의 안전한 스폰 위치를 무작위로 섞어 참가자마다 서로 다르게 배정한다.
- 이동 확인 위치 보정은 0.2미터 이상 오차일 때만 적용해 화면 지터링을 줄인다. 마피아는 크로스헤어로 시민을 조준한 뒤 캔버스를 좌클릭해 처치를 요청하며, 사보타지 지도는 Caps Lock을 누르는 동안만 연다. 시민 발전기 복구 진행은 크로스헤어 아래 진행 막대로 표시된다.
- 이동 확인 좌표는 물리 위치를 직접 덮어쓰지 않고 제한된 보정 속도로 반영한다. 맵은 문 오브젝트 없이 중앙 복도와 좌우 방·출입구 빈 공간으로 구성한 단층 주택 골격이며, 발전기는 서쪽 전력실과 동쪽 전력실에 있다.
- 이동 확인 응답은 로컬 물리 위치와 카메라에 반영하지 않는다. Caps 지도는 해제 또는 Escape 뒤 캔버스 포커스와 포인터 잠금을 즉시 복구한다.
- 로컬 플레이어는 중력 물리 본체를 사용하지 않고 공용 충돌 상자에 따른 수평 예측 위치와 카메라를 함께 갱신한다. 서버는 이동 권한을 계속 검증하지만 로컬 화면 좌표를 되돌리지 않으므로 상하 텔레포팅 경로가 없다.
- 바라보는 방향은 카메라 전방 벡터에서 모델 앞면 기준 yaw로 계산해 전송한다. 서버가 yaw를 -파이부터 파이 범위로 정규화하므로 원격 참가자의 앞·뒷면이 180도 뒤집히지 않는다.
- 2026/08/19/17/47/54 기준 최신 구현: 방 코드 기반 다중 방, 30초 재접속 유예, 서버 권한형 이동·충돌·입력 순번 검증, 수평 완만 좌표 보정, 원격 참가자 방향 동기화가 동작한다.
- 역할·처치·회의·투표·승패와 정전 사보타지가 서버 권한으로 구현됐다. 마피아 처치는 시작 뒤 20초, 처치 뒤 25초 재사용 대기를 적용하고 우측 하단 아이콘에 남은 시간이 표시된다.
- 마피아는 Tab을 누르는 동안 포인터 잠금을 해제해 시설 지도를 열고 발전기 A 또는 B 표식을 클릭한다. 선택한 발전기만 정전 대상이 되며, 시민은 고장 난 동일 발전기를 조준해 E를 3초간 유지해야 복구할 수 있다.
- 최신 변경 기준 자동 테스트 22건과 생산 빌드가 통과했다. Playwright에서 실제 게임 이동 좌표와 375px·768px·1280px HUD를 확인했으며, 검증 브라우저와 테스트 프로세스는 종료된 상태다.
- Node.js WebSocket 게임 서버와 서버 권한형 대기실을 추가했다. 최대 25명 정원, 방장, 준비·시작, 30초 재접속 유예를 제공한다.
- 클라이언트는 대기실에서 접속하고, 초당 15회 이동 입력을 보내며 다른 참가자의 서버 확정 위치를 보간해 표시한다.
- 서버가 이동 시간, 속도, 유한한 입력값을 검증한다. 서버 테스트 5개, 클라이언트 테스트 4개, 생산 빌드가 통과했다.
- 사용자가 다중 브라우저 직접 플레이를 완료했다. 이 세션에는 Playwright MCP가 없어서 페이즈 2 화면 캡처는 저장하지 못했다.
- 역할은 플레이 인원의 20%를 내림해 추천하며, 방장이 직접 값을 설정할 수 있다. 역할 정보는 본인에게만 전송한다.
- 3D 대상 클릭 처치, 긴급 회의, 투표, 건너뛰기, 동점 무추방, 승패 판정을 구현하고 사용자 직접 테스트를 마쳤다.
- 정전, 발전기 복구, 환풍구, 공동 임무 환경 상태를 서버 권한으로 구현했다. 사용자가 정전 상태 UI 동기화를 직접 확인했다.

## 다음 작업

1. MVP 페이즈 1~5가 완료됐다.
2. 실제 배포 환경을 정하면 운영 환경변수와 배포 절차를 추가한다.

## 주의점

- 모든 개선·수정 완료 뒤 관련 변경만 즉시 한국어 커밋으로 남긴다. 다음 작업 시작 전 `git status`로 미커밋 변경을 확인하며, 사용자 기존·무관한 변경은 포함하지 않는다.
- 게임 판정은 서버 권한 방식이어야 한다.
- Playwright MCP가 다시 제공되면 웹 화면 전체 캡처를 먼저 복구해 저장한다.
- 2026/08/20 스폰·맵 확장 변경은 자동 테스트 15개와 생산 빌드를 통과했다. Playwright MCP로 375px·768px·1280px 화면을 촬영해 `docs/screenshots/expanded-map-spawn-*.png`에 저장했고, 실제 2인 방 시작에서 서버 배정 스폰을 확인했다.
- Playwright MCP는 연결됐지만 기존 브라우저 인스턴스가 MCP 프로필을 점유하고 있다. 해당 브라우저를 종료하거나 프로필 잠금이 해제된 뒤 로비·게임·회의·결과 전체 화면을 375px, 768px, 1280px에서 캡처한다.
- Playwright MCP 잠금은 MCP 전용 Chrome 최상위 프로세스를 종료해 해제할 수 있다. 2026/08/19에 로비 화면을 375px, 768px, 1280px에서 캡처해 `docs/screenshots/phase-5-*.png`에 저장했다.
- WebSocket 테스트 클라이언트 25개가 모두 입장해 최대 정원을 검증했다. 모든 개발 서버 포트는 정리됐다.
- 3D 월드는 React Three Fiber와 Rapier React 바인딩으로 복원됐다. 마피아는 처치 거리 안의 시민만 붉은 외곽선으로 보며, 원격 참가자 머리 위에는 배경 상자 없는 이름 텍스트가 표시된다.
- 이번 변경의 자동 테스트와 생산 빌드는 통과했다. Playwright MCP 브라우저 프로필 점유로 3D 화면 캡처는 다시 실행하지 못했다.
- 이번 변경은 `.git` 읽기 전용 제한으로 커밋하지 못했다. 쓰기 권한이 복구되면 관련 파일을 `R3F 처치 강조와 참가자 이름표 적용` 메시지로 커밋한다.
- 게임 시작 때 3D 캔버스가 키보드 포커스와 포인터 잠금을 요청하도록 추가했다. 클라이언트 생산 빌드는 통과했고, 테스트용 5173·2568 포트는 종료했다.
- 발전기 사보타지는 서버 권한으로 정전·CCTV 중단을 전파하며, 시민에게만 4.5미터 카메라 시야 제한과 안개를 적용한다. 복구 또는 새 게임 시작 때 환경 상태를 정상화한다. 자동 테스트 9개와 생산 빌드는 통과했지만 Playwright MCP 프로필 점유로 화면 캡처는 재검증하지 못했다.
- 마피아는 Tab으로 원격 사보타지 패널을 열어 발전기 정전을 실행한다. 시민 복구는 발전기 근처에서 버튼을 3초간 유지해야 하며, 서버가 시작 시각·거리·생존 상태를 검증한다. 최신 서버는 `ws://localhost:2568`에서 실행 중이다.
- Playwright MCP 검증 종료 절차는 탭을 닫은 뒤 `pgrep -af mcp-chrome`으로 MCP 전용 Chrome 최상위 프로세스만 확인·종료하고, 개발 서버·소켓 서버도 함께 종료하는 것이다. 이 절차는 `docs/RULES.md`에 고정했다. 2026/08/19 검증 뒤 2568 서버와 5173 클라이언트, MCP 브라우저 프로필을 모두 종료했다.
- 방장 소켓이 끊겨도 방을 초기화하지 않고 다음 접속자에게 방장 권한을 넘기도록 수정했다. 시민은 발전기를 크로스헤어로 조준하고 E를 3초간 유지해 복구하며, 버튼 기반 복구·환풍구·공동 임무 UI는 제거했다. Playwright로 시민 정전 화면을 375·768·1280px에서 확인하고, 검증 프로세스와 MCP 프로필을 종료했다.
- 맵 벽·내부 장애물을 공용 충돌 상자로 정의해 서버도 이동 충돌을 축별로 해소한다. 정전 중 시민에게만 안개를 무시하는 얇은 발전기 외곽선을 보인다. 자동 테스트 12개와 생산 빌드를 통과했고, 검증 서버·MCP 프로필은 종료했다.
- FPS식 이동으로 서버는 입력 순번을 처리하고 `MOVE_ACK`로 확정 좌표를 돌려준다. 클라이언트는 0.2m 이상 오차만 물리 위치를 보정한다. 방은 6자리 코드로 생성·입장하며, 코드·토큰·이름을 저장해 새로고침 후 같은 방에 재접속한다. 테스트 서버와 MCP 프로필은 종료했다.
- `MOVE_ACK`는 이제 수직 물리 좌표를 덮어쓰지 않고 수평 위치만 완만하게 보정한다. 걷기·달리기 속도도 서버와 클라이언트가 공용 상수를 사용한다. 마피아 처치는 시작 후 20초, 처치 뒤 25초의 서버 권한 재사용 대기를 적용하며 우측 하단 아이콘으로 남은 시간을 표시한다. 자동 테스트 13개와 생산 빌드를 통과했고, Playwright 화면 검증 뒤 테스트 서버와 MCP 프로필을 종료했다.
- 원격 참가자 모델은 서버가 확정한 수평 회전을 반영하고, 밝은 앞면 표시로 바라보는 방향을 구분한다. 자동 테스트 13개와 생산 빌드를 통과했으며 Playwright 검증 뒤 테스트 서버와 MCP 프로필을 종료했다.
- 마피아는 Tab을 누르는 동안 포인터가 나타나는 시설 지도를 열어 발전기 A 또는 B 표식을 클릭해 원격 방해공작을 실행한다. 선택한 발전기만 고장 상태가 되고 시민은 같은 발전기에 크로스헤어를 두고 E를 3초간 유지해야 복구할 수 있다. 서버는 발전기 대상과 거리·역할·유지 시간을 검증한다. 자동 테스트 14개와 생산 빌드를 통과했고, Playwright 검증 뒤 테스트 서버와 MCP 프로필을 종료했다.
- 마피아 수, 맵 구조, 세부 수치처럼 문서에 없는 큰 설계 결정은 사용자 확인 후 정한다.
- 생산 빌드의 초기 JavaScript 번들은 압축 전 약 3.3MB이므로 이후 화면 분할을 검토한다.
